name: example
description: log_collector example.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  log_collector:
    path: ../../log_collector
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
