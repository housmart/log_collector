name: log_collector
description: Provides log filtering, buffering, and retry.
author: Ryo Miyake / Housmart Inc. <ryo.miyake@housmart.com>
homepage: https://github.com/housmart/log_collector
version: 1.0.1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^1.2.0
  synchronized: ^2.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
